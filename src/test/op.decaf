class Program {
    int a;
    int b;

    int main() {
        int x, y;
        boolean v, w;
        v = true;
        a = 10;
        b = 20;
        x = -1 + b * (10 / a+b);
        y = !v && x > b;

        callout ("printf","%d %d %s", x, y, w);

    }

}
